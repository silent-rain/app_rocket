

table! {
    user (id) {
        id -> Integer,
        name -> Varchar,
        gender -> Bool,
        age -> Integer,
        birth -> Varchar,
        phone -> Varchar,
        email -> Varchar,
        password -> Varchar,
        address -> Varchar,
        avatar -> Varchar,
        status -> Bool,
        created -> Varchar,
        updated -> Varchar,
    }
}
